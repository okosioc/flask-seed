{% set model = seed.model %}
{% set sub = seed.sub %}
{% if seed.action in ['read', 'summary', 'form', 'query'] %}
    {% include '#model_' + seed.action + '.html.jinja2' %}
{% else %}
    TODO: unsupport action {{ seed.action }}
{% endif %}